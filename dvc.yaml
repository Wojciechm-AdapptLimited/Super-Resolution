stages:
  train:
    cmd: python scripts/train.py unet -E 15
    deps:
    - data/kaggle
    - scripts/train.py
    metrics:
    - metrics.json:
        cache: false
    plots:
    - loss.csv:
        cache: false
